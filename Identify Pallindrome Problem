n=str(input())

def check_Pallindrome(n):
    if n==n[::-1]:
        return True
    else:
        return False

while True:
    sum=0
    n=str(n)
    sum=int(n)+int(n[::-1])
    if check_Pallindrome(str(sum)):
        break
    n=sum

print(sum)
    
